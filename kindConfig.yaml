kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 30001  # This is the NodePort from your frontend service
    hostPort: 3001        # Access the app on http://localhost:3001 from Windows
    protocol: TCP
- role: worker
- role: worker
- role: worker